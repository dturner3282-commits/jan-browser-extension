Place small sample documents here for quick testing on the offline machine:
- example.pdf  (small PDF)
- example.txt  (simple text file)
- example.md   (markdown file)

These are optional and just make the first validation steps faster.
