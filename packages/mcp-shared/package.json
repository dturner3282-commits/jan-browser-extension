{
  "name": "@janhq/mcp-shared",
  "version": "0.0.0",
  "private": true,
  "description": "Shared types and utilities for MCP packages (internal use only)",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "zod": "^3.24.1",
    "zod-to-json-schema": "^3.24.1"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  },
  "license": "MIT"
}
