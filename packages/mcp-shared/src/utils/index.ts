/**
 * Utility functions
 */

export {
  sanitizeClickParams,
  sanitizeTypeParams,
  sanitizeInputParams,
  sanitizeDragParams,
  sanitizeNavigateParams,
  sanitizeScrollParams,
  sanitizeSnapshotParams,
  sanitizeScreenshotParams,
} from './sanitize.js';
